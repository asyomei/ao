---
interface Props {
  title?: string
}

const { title } = Astro.props
---

<section class="section">
  {title && <h2>--- {title} ---</h2>}

  <slot />
</section>

<style is:global>
  .section {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }
</style>
